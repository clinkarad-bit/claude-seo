# CLAUDE.md

This file provides guidance for AI assistants working with the `claude-seo` repository.

## Project Overview

**Repository:** clinkarad-bit/claude-seo
**Status:** Newly initialized — the project is in its earliest stage with no source code yet.

## Repository Structure

```
claude-seo/
├── CLAUDE.md          # AI assistant guidance (this file)
└── .git/              # Git repository metadata
```

This repository is empty and awaiting initial project setup. As the project grows, update this file to reflect new directories, configuration, and conventions.

## Development Workflow

### Branch Conventions

- Development branches follow the pattern `claude/<descriptor>`
- Push with: `git push -u origin <branch-name>`

### Getting Started

Once the project is bootstrapped, document here:
- How to install dependencies
- How to run the dev server
- How to run tests
- How to build for production

## Code Conventions

_To be defined as the project takes shape._ Document conventions here as they are established, including:

- Language and framework choices
- File and directory naming
- Import ordering
- Formatting and linting tools
- Testing patterns

## Key Commands

_To be populated once tooling is configured._ Example entries:

```
# Install dependencies
# <command>

# Run development server
# <command>

# Run tests
# <command>

# Lint / format
# <command>

# Build
# <command>
```

## Architecture Notes

_To be defined._ When architectural decisions are made, record them here so AI assistants understand the project's design rationale.

## Environment & Configuration

_To be defined._ Document any required environment variables, config files, or secrets setup needed for local development.

## Guidelines for AI Assistants

1. **Read before editing** — Always read a file before proposing changes.
2. **Minimal changes** — Only modify what is directly requested; avoid unnecessary refactoring.
3. **Keep this file current** — Update CLAUDE.md whenever significant project structure, tooling, or conventions change.
4. **Security first** — Do not commit secrets, credentials, or `.env` files.
5. **Test your changes** — Run the project's test suite (once established) before considering a task complete.
